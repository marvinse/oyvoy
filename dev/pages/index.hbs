{{>head}}
	{{>menu}}
	<div class="container">
		{{>banner bannerclass='top'}}
		{{>slider-restaurants}}
		<div class="clear"></div>
		{{>banner bannerclass='bottom'}}
		<div class="popup"></div>
	</div>
	{{>full-modal}}
{{>foot}}